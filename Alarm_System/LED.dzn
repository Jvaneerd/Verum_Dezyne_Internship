import ILED.dzn;

component LED {
	provides ILED iLED;
	
	behaviour {
		enum State { Green, Yellow, Red };
		State state = State.Green;
		on iLED.setGreen(): {
			state = State.Green;
		}
		on iLED.setYellow(): {
			state = State.Yellow;
		}
		on iLED.setRed(): {
			state = State.Red;
		}
	}
}